englishonly: false

# zeebe.client.cloud:
#   clusterId: YOUR_CLUSTER_ID
#   clientId: YOUR_CLIENT_ID
#   clientSecret: YOUR_CLIENT_SECRET

zeebe.client.cloud:
      region: bru-2
      clusterId: ${CLIENT_CLOUD_CLUSTER_ID}
      clientId: ${CLIENT_CLOUD_CLIENT_ID}
      clientSecret: ${CLIENT_CLOUD_CLIENT_SECRET}
      defaultName: presalesDemoExecutor

server:
  servlet:
    context-path: /
